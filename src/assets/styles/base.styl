/* 全局样式 */
* 
  margin 0
  padding 0
  box-sizing border-box

html, body, #app {
    width 100%
  height: 100%;
  margin: 0;
  padding: 0;
  overflow: hidden; /* 防止额外的滚动条 */
}
body
  font-family 'Arial', sans-serif
#app {
  margin: 0;
  padding: 0;
  background-color: #f0f2f5;
  max-width: 100%
}

.tx-center
  text-align center

.flex
  display: flex
.flex-center
  justify-content center
  align-items center